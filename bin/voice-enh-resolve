#!/bin/sh
set -eu
if [ "$#" -lt 1 ]; then
  echo "voice-enh-resolve: Resolve did not provide an input file" >&2
  exit 2
fi
PROJECT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
exec "$PROJECT_DIR/.venv/bin/python" -m voice_enh --resolve "$1"
